# MediPill AI 프로젝트 목표 및 사용자 요구사항 분석 문서

## 1. 프로젝트 개요

MediPill AI는 사용자가 의약품 복용과 관련된 질문을 입력하면, 사용자의 건강정보를 참고하여 AI가 복용 시 주의사항과 상담 방향을 안내하는 Web Application이다.

이 프로젝트는 단순한 챗봇 구현이 아니라, 웹페이지 요구사항 정리, 화면 설계, 프론트엔드와 Django 백엔드 개발, LLM 연동, 클라우드 배포까지 포함하는 전체 웹 애플리케이션 개발 과정을 목표로 한다.

## 2. 프로젝트 목표

### 2.1 웹페이지 개발을 위한 요구사항 정리 및 화면 설계

사용자가 서비스를 이용하는 흐름을 기준으로 필요한 화면과 기능을 정의한다.

- 회원가입 및 로그인 화면
- 건강정보 등록 및 수정 화면
- AI 상담 화면
- 상담 이력 조회 화면
- 사용자 입력, 응답, 오류 상황에 대한 UI 흐름

### 2.2 HTML, CSS, JavaScript 기반 프론트엔드와 Django 기반 백엔드 개발

React 기반 프론트엔드와 Django REST API 기반 백엔드를 분리하여 Web Application을 구축한다.

- 프론트엔드: 사용자 화면, 입력 폼, API 요청 처리
- 백엔드: 회원 관리, 인증, 건강정보 저장, 상담 요청 처리
- API 통신: Axios를 활용한 프론트엔드와 백엔드 연동

### 2.3 LLM 연동 기능을 추가한 Web Application 구현

OpenAI API를 연동하여 사용자의 질문에 자연어 답변을 제공한다.

- 사용자의 질문을 백엔드로 전달
- 사용자의 건강 프로필 정보를 프롬프트에 포함
- LLM 응답을 상담 답변 형태로 반환
- 의료 판단을 대체하지 않는 안내 문구 포함

### 2.4 클라우드 활용 인프라 구축 및 배포

로컬 환경에서 동작하는 애플리케이션을 클라우드 환경에서 접근 가능한 서비스로 확장한다.

- 프론트엔드 정적 배포
- Django API 서버 배포
- 운영 DB 구성
- 환경변수 및 API Key 관리
- HTTPS, CORS, 도메인 설정

## 3. 사용자 요구사항 분석

## 3.1 주요 사용자

주요 사용자는 의약품 복용 전 주의사항을 확인하고 싶은 일반 사용자이다.

사용자는 다음과 같은 상황에서 서비스를 사용할 수 있다.

- 특정 약을 공복에 먹어도 되는지 궁금한 경우
- 감기약, 진통제, 항생제 등 약 복용 시 주의사항을 확인하고 싶은 경우
- 본인의 알레르기, 질환, 복용약에 따라 주의할 점이 다른지 알고 싶은 경우
- 이전 상담 내용을 다시 확인하고 싶은 경우

## 3.2 사용자 문제

사용자가 겪는 문제는 다음과 같다.

- 의약품 정보를 검색해도 내용이 어렵고 흩어져 있다.
- 개인 건강상태에 따라 주의해야 할 점을 스스로 판단하기 어렵다.
- 약 복용과 관련된 질문을 자연어로 편하게 물어볼 수 있는 창구가 부족하다.
- 이전 상담 내용을 다시 확인하기 어렵다.

## 3.3 사용자 니즈

사용자는 다음과 같은 기능을 필요로 한다.

- 쉽게 질문할 수 있는 채팅형 상담 화면
- 본인의 건강정보를 반영한 개인화된 답변
- 복용 시 주의사항 중심의 간단하고 읽기 쉬운 답변
- 로그인 기반 개인 상담 이력 저장
- 정확한 의료 판단이 필요한 경우 전문가 상담 권고

## 4. 기능 요구사항

## 4.1 회원가입 기능

사용자는 계정을 생성할 수 있어야 한다.

- 사용자명, 이메일, 비밀번호 입력
- 중복 사용자명 검증
- 회원가입 완료 메시지 제공

## 4.2 로그인 및 인증 기능

사용자는 로그인 후 개인 기능을 사용할 수 있어야 한다.

- JWT 기반 로그인
- Access Token 저장
- 인증이 필요한 API 요청 시 Authorization 헤더 포함
- 로그아웃 시 토큰 삭제

## 4.3 건강정보 관리 기능

사용자는 AI 상담에 활용될 건강정보를 등록하고 수정할 수 있어야 한다.

필요 데이터는 다음과 같다.

- 나이
- 성별
- 키
- 몸무게
- 혈액형
- 임신 또는 수유 여부
- 알레르기 정보
- 기저질환
- 현재 복용 중인 약

이 기능은 단순한 프로필 기능이 아니라 AI 상담의 입력 맥락을 만드는 핵심 보조 기능이다.

## 4.4 AI 상담 기능

사용자는 의약품 복용과 관련된 질문을 입력하고 AI 답변을 받을 수 있어야 한다.

상담 기능은 다음 흐름으로 동작한다.

1. 사용자가 질문 입력
2. 백엔드가 사용자 건강정보 조회
3. 질문과 건강정보를 조합하여 프롬프트 구성
4. OpenAI API 호출
5. 답변 반환
6. 질문과 답변을 상담 이력으로 저장

답변에는 다음 내용이 포함되는 것이 적절하다.

- 간단 답변
- 개인 건강정보 기반 주의사항
- 추가 확인이 필요한 정보
- 의사 또는 약사 상담 권고

## 4.5 상담 이력 조회 기능

사용자는 이전 상담 내용을 다시 확인할 수 있어야 한다.

- 사용자별 상담 이력 저장
- 질문, 답변, 생성 시각 조회
- 최신 상담순 정렬

## 4.6 RAG 기반 문서 검색 기능

의약품 문서나 DUR 데이터를 기반으로 답변 근거를 강화할 수 있어야 한다.

현재 프로젝트에는 RAG 구조가 설계되어 있으며 다음 기능이 포함된다.

- 질문 의도 분석
- 검색 계획 생성
- FAISS 벡터 검색
- BM25 키워드 검색
- LLM 기반 문서 선별
- 문서 기반 답변 생성

다만 현재 웹앱의 메인 상담 API와 완전히 통합되지는 않았으며, 데이터 전처리 결과와 벡터 DB 준비가 추가로 필요하다.

## 5. 비기능 요구사항

## 5.1 보안 요구사항

- OpenAI API Key는 코드에 직접 작성하지 않고 환경변수로 관리해야 한다.
- Django SECRET_KEY도 배포 환경에서는 환경변수로 분리해야 한다.
- 로그인 사용자의 개인 건강정보는 인증된 사용자만 접근할 수 있어야 한다.

## 5.2 안정성 요구사항

- OpenAI API Key가 없더라도 서버가 시작되지 못하는 문제는 없어야 한다.
- LLM 호출 실패 시 사용자에게 기본 안내 응답을 제공해야 한다.
- 필수 입력값이 누락되면 명확한 오류 메시지를 제공해야 한다.

## 5.3 사용성 요구사항

- 사용자가 바로 질문할 수 있도록 예시 질문을 제공해야 한다.
- 예시 질문과 입력창 문구는 한국어 사용자에게 맞게 제공해야 한다.
- 로딩 중 상태를 표시해야 한다.
- 상담 결과는 질문과 답변이 구분되어 보여야 한다.

## 5.4 확장성 요구사항

- 프론트엔드 API 주소는 배포 환경에 따라 변경 가능해야 한다.
- SQLite는 개발용으로 사용하고, 배포 환경에서는 PostgreSQL 등 운영 DB로 전환하는 것이 적절하다.
- RAG 데이터와 벡터 DB는 별도 저장소 또는 배포 자원으로 관리할 수 있어야 한다.

## 6. 화면 설계 요구사항

## 6.1 로그인 화면

목적은 사용자가 개인 기능에 접근하기 위한 인증을 수행하는 것이다.

필요 요소:

- 아이디 입력
- 비밀번호 입력
- 로그인 버튼
- 회원가입 화면 이동
- 로그인 실패 메시지

## 6.2 회원가입 화면

목적은 신규 사용자가 서비스를 이용할 수 있도록 계정을 생성하는 것이다.

필요 요소:

- 사용자명 입력
- 이메일 입력
- 비밀번호 입력
- 회원가입 버튼
- 중복 계정 또는 입력 오류 안내

## 6.3 건강정보 화면

목적은 AI 상담에 필요한 개인 건강정보를 등록하거나 수정하는 것이다.

필요 요소:

- 기본 신체 정보 입력
- 알레르기, 질환, 복용약 입력
- 임신 또는 수유 여부 선택
- 저장 및 수정 버튼

## 6.4 AI 상담 화면

목적은 사용자가 의약품 관련 질문을 입력하고 AI 답변을 확인하는 것이다.

필요 요소:

- 건강정보 요약 표시
- 한국어 예시 질문 버튼
- 질문 입력창
- 질문하기 버튼
- 로딩 표시
- AI 답변 표시
- 상담 이력 화면 이동

## 6.5 상담 이력 화면

목적은 사용자가 이전 질문과 답변을 다시 확인하는 것이다.

필요 요소:

- 질문 목록
- 답변 내용
- 상담 생성 시각
- 최신순 정렬

## 7. 시스템 구조 요구사항

## 7.1 프론트엔드

프론트엔드는 사용자의 입력과 화면 흐름을 담당한다.

- React 기반 SPA
- React Router를 통한 페이지 이동
- Axios 기반 API 요청
- JWT 토큰을 localStorage에 저장
- 인증 API 요청 시 Bearer Token 포함

## 7.2 백엔드

백엔드는 API와 데이터 처리를 담당한다.

- Django 기반 프로젝트
- Django REST Framework 사용
- SimpleJWT 인증
- accounts, profiles, chats 앱 분리
- SQLite 기반 개발 DB

## 7.3 LLM 연동

LLM 연동은 백엔드에서 처리한다.

- 프론트엔드는 질문만 백엔드에 전달
- 백엔드는 건강정보와 질문을 조합
- OpenAI API 호출
- 응답 결과를 저장 후 프론트에 반환

## 8. 배포 요구사항

## 8.1 프론트엔드 배포

프론트엔드는 정적 파일로 빌드하여 배포한다.

가능한 배포 대상:

- Vercel
- Netlify
- AWS S3 + CloudFront

필요 작업:

- `npm run build`
- API Base URL 환경변수화
- HTTPS 주소 기준으로 백엔드 API 호출

## 8.2 백엔드 배포

Django 백엔드는 API 서버로 배포한다.

가능한 배포 대상:

- Render
- Railway
- Fly.io
- AWS EC2

필요 작업:

- `DEBUG=False`
- `ALLOWED_HOSTS` 설정
- `CORS_ALLOWED_ORIGINS` 설정
- Gunicorn 또는 배포 플랫폼 실행 명령 구성
- 환경변수 등록

## 8.3 데이터베이스 배포

개발 환경에서는 SQLite를 사용할 수 있지만, 배포 환경에서는 PostgreSQL 사용이 적절하다.

필요 작업:

- PostgreSQL 인스턴스 생성
- `DATABASE_URL` 환경변수 설정
- Django DB 설정 변경
- 마이그레이션 실행

## 8.4 비밀키 관리

배포 환경에서는 다음 값을 환경변수로 관리해야 한다.

- `OPENAI_API_KEY`
- `SECRET_KEY`
- `DATABASE_URL`
- 기타 배포 도메인 관련 설정

## 9. 현재 구현 범위

현재 프로젝트에서 확인된 구현 범위는 다음과 같다.

| 구분 | 상태 | 설명 |
|---|---|---|
| 회원가입 | 구현 | Django User 기반 회원가입 API 존재 |
| 로그인 | 구현 | SimpleJWT 기반 토큰 발급 |
| 건강정보 관리 | 구현 | 사용자별 HealthProfile 모델 및 API 존재 |
| AI 상담 | 구현 | OpenAI API 연동 및 상담 이력 저장 |
| 상담 이력 | 구현 | 사용자별 상담 내역 조회 API 존재 |
| 프론트 화면 | 구현 | Login, Signup, Profile, Chat, History 화면 존재 |
| RAG 문서검색 | 부분 구현 | 체인 구조는 있으나 웹앱 통합과 데이터 준비 필요 |
| 클라우드 배포 | 미구현 | 배포 설정 및 운영 환경 구성 필요 |

## 10. 아쉬운 점 및 개선 방향

## 10.1 RAG 통합 부족

RAG 체인은 별도 코드로 존재하지만, Django의 메인 AI 상담 API와 완전히 통합되어 있지 않다.

개선 방향:

- RAG 검색 결과를 Django Chat API에서 사용
- 답변에 참고 문서 또는 출처 표시
- FAISS 벡터 DB 생성 및 배포 자원 관리

## 10.2 답변 신뢰성 검증 부족

현재 LLM 답변은 프롬프트에 의존하는 구조이다.

개선 방향:

- DUR 데이터 기반 위험성 검증
- 약물 상호작용 체크 로직 추가
- 답변 근거 문서 표시

## 10.3 사용자 피드백 기능 부족

사용자가 답변이 도움이 되었는지 평가하는 기능이 없다.

개선 방향:

- 답변 만족도 평가
- 부정확한 답변 신고
- 관리자 검토 기능

## 10.4 배포 환경 구성 부족

로컬 실행 중심으로 구성되어 있으며 클라우드 운영 설정이 부족하다.

개선 방향:

- 프론트엔드와 백엔드 분리 배포
- PostgreSQL 전환
- 환경변수 기반 설정 관리
- 배포 후 API 통신 테스트

## 11. 최종 정리

MediPill AI의 핵심 목표는 사용자가 의약품 복용과 관련된 질문을 쉽게 입력하고, 자신의 건강정보를 반영한 AI 상담 답변을 받을 수 있도록 하는 것이다.

현재 프로젝트는 회원가입, 로그인, 건강정보 관리, AI 상담, 상담 이력 조회 등 Web Application의 핵심 기능을 갖추고 있다.

다만 프로젝트 목표를 완전히 달성하기 위해서는 RAG 기반 문서검색 기능을 메인 상담 기능과 통합하고, 클라우드 배포 환경을 구축하며, 답변 신뢰성과 사용자 피드백 기능을 보완해야 한다.
