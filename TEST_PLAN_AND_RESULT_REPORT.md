# MediPill AI 테스트 계획 및 결과 보고서

## 1. 문서 개요

본 문서는 MediPill AI Web Application의 주요 기능에 대한 테스트 계획과 테스트 결과를 정리한 보고서이다.

테스트 대상은 회원가입, 로그인, 건강정보 관리, AI 상담, 상담 이력 조회, LLM 연동, 예외 처리 기능이다. 특히 사용자가 질문을 입력하고, 백엔드 API 요청을 통해 LLM 답변을 받아 화면에 출력하는 핵심 흐름이 정상적으로 동작하는지 검증하는 것을 목표로 한다.

## 2. 테스트 목적

테스트의 목적은 다음과 같다.

- 주요 API가 정상적으로 요청과 응답을 처리하는지 확인한다.
- 사용자 질문 입력부터 AI 답변 출력까지의 핵심 서비스 흐름을 검증한다.
- 인증이 필요한 기능에서 JWT 토큰이 정상적으로 사용되는지 확인한다.
- 빈 입력, 잘못된 요청, 인증 누락, LLM 응답 실패 등 예외 상황을 확인한다.
- 테스트 중 발견된 문제점과 개선 사항을 정리한다.
9
## 3. 테스트 환경

| 항목 | 내용 |
|---|---|
| 프로젝트명 | MediPill AI |
| 프론트엔드 | React, Vite, Axios |
| 백엔드 | Django, Django REST Framework |
| 인증 방식 | SimpleJWT |
| 데이터베이스 | SQLite |
| LLM 연동 | OpenAI API |
| 로컬 백엔드 주소 | `http://127.0.0.1:8000` |
| 로컬 프론트엔드 주소 | `http://localhost:5173` |
| 테스트 도구 | 브라우저, PowerShell, API 요청 도구 |

## 4. 테스트 범위

## 4.1 테스트 대상 기능

| 구분 | 테스트 대상 |
|---|---|
| 사용자 인증 | 회원가입, 로그인, JWT 토큰 발급 |
| 건강정보 관리 | 건강정보 등록, 조회, 수정 |
| AI 상담 | 질문 입력, API 요청, LLM 응답 처리, 답변 출력 |
| 상담 이력 | 질문과 답변 저장, 상담 이력 조회 |
| 예외 처리 | 빈 입력, 인증 누락, 잘못된 요청, LLM 실패 |
| UI 표시 | 한국어 예시 질문, 입력창, 로딩, 오류 안내 |

## 4.2 테스트 제외 또는 제한 범위

다음 항목은 현재 프로젝트 일정과 환경상 제한적으로 확인한다.

- 실제 클라우드 배포 환경 테스트
- PostgreSQL 운영 DB 전환 테스트
- RAG 기반 문서 검색 결과의 실제 통합 테스트
- OpenAI API 사용량 또는 결제 제한으로 인한 실제 LLM 응답 생성 테스트

## 5. 테스트 방법

## 5.1 API 테스트

백엔드 API는 다음 방식으로 테스트한다.

- Django 개발 서버 실행
- 회원가입 API 요청
- 로그인 API 요청 및 JWT 토큰 발급 확인
- 발급받은 Access Token을 Authorization 헤더에 포함
- 건강정보 등록 및 조회 요청
- AI 상담 질문 요청
- 상담 이력 조회 요청

## 5.2 화면 테스트

프론트엔드 화면은 다음 방식으로 테스트한다.

- 브라우저에서 각 페이지 접근
- 입력창에 값 입력
- 버튼 클릭 시 API 요청 발생 여부 확인
- 로딩 상태 표시 확인
- 답변 또는 오류 메시지 표시 확인
- 한국어 문구 표시 확인

## 5.3 예외 테스트

예외 케이스는 다음 방식으로 테스트한다.

- 빈 질문으로 상담 요청
- 토큰 없이 인증 API 요청
- 건강정보 미등록 상태에서 상담 화면 접근
- OpenAI API Key 누락 상태에서 서버 실행 확인
- OpenAI API 호출 실패 시 기본 안내 응답 처리 확인

## 6. 기능별 테스트 항목

## 6.1 회원가입 테스트

| 테스트 ID | 테스트 항목 | 테스트 방법 | 예상 결과 | 결과 |
|---|---|---|---|---|
| TC-001 | 정상 회원가입 | 사용자명, 이메일, 비밀번호 입력 후 회원가입 요청 | 계정 생성 및 user_id 반환 | 성공 |
| TC-002 | 중복 사용자명 | 이미 존재하는 사용자명으로 회원가입 요청 | 중복 오류 메시지 반환 | 확인 필요 |
| TC-003 | 필수값 누락 | 사용자명 또는 비밀번호 누락 후 요청 | 오류 메시지 반환 | 확인 필요 |

## 6.2 로그인 테스트

| 테스트 ID | 테스트 항목 | 테스트 방법 | 예상 결과 | 결과 |
|---|---|---|---|---|
| TC-004 | 정상 로그인 | 올바른 사용자명과 비밀번호 입력 | access token, refresh token 반환 | 성공 |
| TC-005 | 잘못된 로그인 | 잘못된 비밀번호 입력 | 인증 실패 응답 반환 | 확인 필요 |
| TC-006 | 토큰 저장 | 로그인 성공 후 프론트 localStorage 확인 | accessToken 저장 | 성공 |

## 6.3 건강정보 테스트

| 테스트 ID | 테스트 항목 | 테스트 방법 | 예상 결과 | 결과 |
|---|---|---|---|---|
| TC-007 | 건강정보 등록 | 인증 토큰 포함 후 건강정보 POST 요청 | 건강정보 저장 성공 | 성공 |
| TC-008 | 건강정보 조회 | 인증 토큰 포함 후 GET 요청 | 저장된 건강정보 반환 | 성공 |
| TC-009 | 건강정보 수정 | 인증 토큰 포함 후 PUT 요청 | 수정된 건강정보 반환 | 성공 |
| TC-010 | 인증 누락 | 토큰 없이 건강정보 API 요청 | 인증 오류 반환 | 확인 필요 |

## 6.4 AI 상담 테스트

| 테스트 ID | 테스트 항목 | 테스트 방법 | 예상 결과 | 결과 |
|---|---|---|---|---|
| TC-011 | 정상 질문 입력 | 질문 입력 후 상담 API 요청 | AI 답변 반환 및 화면 출력 | 부분 성공 |
| TC-012 | 상담 이력 저장 | 상담 요청 후 DB 저장 여부 확인 | 질문과 답변 저장 | 성공 |
| TC-013 | 상담 이력 조회 | `/api/chat/history/` 요청 | 이전 상담 목록 반환 | 성공 |
| TC-014 | 빈 질문 입력 | 빈 문자열로 상담 요청 | 질문 입력 요청 오류 반환 | 성공 |
| TC-015 | LLM API 실패 | API Key 누락 또는 quota 오류 상황에서 요청 | 기본 안내 응답 또는 오류 처리 | 성공 |

## 6.5 프론트엔드 화면 테스트

| 테스트 ID | 테스트 항목 | 테스트 방법 | 예상 결과 | 결과 |
|---|---|---|---|---|
| TC-016 | 예시 질문 표시 | AI 상담 화면 확인 | 한국어 예시 질문 3개 표시 | 성공 |
| TC-017 | 질문 입력창 표시 | AI 상담 화면 확인 | 한국어 placeholder 표시 | 성공 |
| TC-018 | 질문하기 버튼 | 질문 입력 후 버튼 클릭 | API 요청 발생 | 성공 |
| TC-019 | 로딩 상태 표시 | 질문 전송 후 응답 전 화면 확인 | `생성 중...` 표시 | 성공 |
| TC-020 | 답변 출력 | 상담 응답 수신 후 화면 확인 | Q/A 형태로 답변 표시 | 성공 |

## 7. 핵심 기능 테스트 결과

## 7.1 사용자 질문 입력 테스트

| 항목 | 내용 |
|---|---|
| 테스트 내용 | 사용자가 AI 상담 화면에서 질문을 입력한다. |
| 입력 예시 | `타이레놀을 공복에 먹어도 되나요?` |
| 예상 결과 | 질문이 프론트엔드 state에 저장되고, 질문하기 버튼 클릭 시 API 요청에 포함된다. |
| 결과 | 성공 |

## 7.2 API 요청 테스트

| 항목 | 내용 |
|---|---|
| 테스트 내용 | 프론트엔드에서 `/api/chat/`으로 POST 요청을 전송한다. |
| 요청 데이터 | `{ "question": "타이레놀을 공복에 먹어도 되나요?" }` |
| 인증 방식 | Bearer Access Token |
| 예상 결과 | 백엔드가 로그인 사용자를 확인하고 질문을 처리한다. |
| 결과 | 성공 |

## 7.3 LLM 응답 출력 테스트

| 항목 | 내용 |
|---|---|
| 테스트 내용 | 백엔드가 OpenAI API 응답 또는 기본 안내 응답을 프론트엔드로 반환한다. |
| 예상 결과 | 화면에 질문과 답변이 Q/A 형태로 표시된다. |
| 결과 | 부분 성공 |
| 비고 | OpenAI API 사용량 또는 결제 제한이 있는 경우 실제 LLM 답변 대신 기본 안내 응답이 표시될 수 있다. |

## 8. 예외 케이스 테스트 결과

## 8.1 빈 입력 테스트

| 항목 | 내용 |
|---|---|
| 테스트 상황 | 질문 입력창이 비어 있는 상태에서 질문하기 버튼 클릭 |
| 예상 결과 | 질문을 입력하라는 안내 메시지 표시 |
| 결과 | 성공 |

## 8.2 인증 누락 테스트

| 항목 | 내용 |
|---|---|
| 테스트 상황 | JWT 토큰 없이 건강정보 또는 상담 API 요청 |
| 예상 결과 | 인증 오류 응답 반환 |
| 결과 | 확인 필요 |
| 개선 사항 | API 테스트 도구로 401 응답 여부를 추가 확인해야 한다. |

## 8.3 OpenAI API Key 누락 테스트

| 항목 | 내용 |
|---|---|
| 테스트 상황 | `OPENAI_API_KEY`가 없는 상태에서 Django 서버 실행 |
| 기존 문제 | 서버 시작 시 OpenAI 클라이언트를 바로 생성하여 서버가 실행되지 않음 |
| 수정 내용 | OpenAI 클라이언트를 요청 시점에 생성하도록 변경 |
| 수정 후 결과 | `python manage.py check` 통과 |
| 결과 | 성공 |

## 8.4 LLM 응답 실패 테스트

| 항목 | 내용 |
|---|---|
| 테스트 상황 | OpenAI API 호출 실패 또는 quota 부족 |
| 발생 가능 오류 | `insufficient_quota`, API Key 누락, 네트워크 오류 |
| 예상 결과 | 서비스가 중단되지 않고 기본 안내 응답 반환 |
| 결과 | 성공 |

## 8.5 건강정보 미등록 테스트

| 항목 | 내용 |
|---|---|
| 테스트 상황 | 건강정보가 등록되지 않은 사용자가 상담 화면 접근 |
| 예상 결과 | 건강정보 미등록 안내와 등록 버튼 표시 |
| 결과 | 성공 |

## 9. 발견된 문제점

## 9.1 OpenAI API Key 누락 시 서버 시작 실패

| 항목 | 내용 |
|---|---|
| 문제 | `chats/views.py` import 시점에 `OpenAI(api_key=...)`를 생성하여 API Key가 없으면 Django 서버가 시작되지 않음 |
| 영향 | `python manage.py runserver` 실행 실패 |
| 원인 | OpenAI 클라이언트 생성 시점이 서버 시작 단계였음 |
| 조치 | `get_openai_client()` 함수를 추가하고 실제 채팅 요청 시점에 클라이언트를 생성하도록 수정 |
| 결과 | `python manage.py check` 통과 |

## 9.2 `.env` 파일 경로 문제

| 항목 | 내용 |
|---|---|
| 문제 | 기존 코드가 `backend/.env`만 읽도록 구성되어 프로젝트 루트 `.env`의 API Key를 읽지 못할 수 있음 |
| 영향 | `.env` 파일이 존재해도 API Key 누락 오류 발생 가능 |
| 조치 | 프로젝트 루트 `.env`와 `backend/.env`를 모두 읽도록 수정 |
| 결과 | 환경변수 로딩 안정성 개선 |

## 9.3 OpenAI API quota 제한

| 항목 | 내용 |
|---|---|
| 문제 | OpenAI API 호출 시 사용량 또는 결제 제한으로 `insufficient_quota` 오류 발생 가능 |
| 영향 | 실제 LLM 답변 생성 제한 |
| 조치 | LLM 호출 실패 시 기본 안내 응답을 반환하도록 예외 처리 |
| 결과 | 서비스 흐름은 유지되지만 실제 답변 품질 검증은 제한됨 |

## 9.4 RAG 데이터 미준비

| 항목 | 내용 |
|---|---|
| 문제 | `data/processed`와 `vectorstore/faiss_index`가 준비되지 않은 상태 |
| 영향 | RAG 문서 검색 기능을 실제 상담 흐름에서 검증하기 어려움 |
| 조치 필요 | 전처리 데이터 생성, FAISS 인덱스 생성, Django Chat API와 RAG 통합 |
| 결과 | 후속 개선 과제로 분류 |

## 9.5 영어 예시 질문 표시

| 항목 | 내용 |
|---|---|
| 문제 | AI 상담 화면의 예시 질문과 placeholder가 영어로 표시됨 |
| 영향 | 한국어 사용자 입장에서 사용성이 낮아짐 |
| 조치 | 예시 질문과 placeholder를 한국어로 수정 |
| 결과 | 한국어 UI 요구사항 충족 |

## 10. 수정 및 개선 사항

| 번호 | 개선 항목 | 수정 내용 | 상태 |
|---|---|---|---|
| 1 | OpenAI 클라이언트 생성 시점 변경 | 서버 시작 시점이 아닌 채팅 요청 시점에 생성 | 완료 |
| 2 | `.env` 로딩 경로 개선 | 프로젝트 루트와 backend 폴더의 `.env` 모두 로딩 | 완료 |
| 3 | LLM 실패 예외 처리 | 실패 시 기본 안내 응답 반환 | 완료 |
| 4 | 한국어 예시 질문 적용 | Chat 화면 예시 질문과 placeholder 한국어 변경 | 완료 |
| 5 | RAG 통합 | Django Chat API와 RAG 연결 필요 | 미완료 |
| 6 | 배포 환경 테스트 | 클라우드 배포 후 API 통신 테스트 필요 | 미완료 |

## 11. 테스트 결론

MediPill AI의 주요 기능인 회원가입, 로그인, 건강정보 등록 및 조회, AI 상담 요청, 상담 이력 저장 및 조회 기능은 로컬 환경에서 MVP 수준으로 동작 가능하도록 구현되어 있다.

사용자 질문 입력, 백엔드 API 요청, 답변 출력 흐름은 정상적으로 설계되어 있으며, OpenAI API Key 누락 또는 LLM 호출 실패 상황에서도 서버가 중단되지 않도록 개선하였다.

다만 실제 LLM 응답 품질 검증은 OpenAI API 사용량 또는 결제 제한에 영향을 받을 수 있으며, RAG 기반 문서 검색과 클라우드 배포 테스트는 아직 후속 작업이 필요하다.

## 12. 향후 테스트 계획

향후에는 다음 테스트를 추가로 수행해야 한다.

| 테스트 항목 | 목적 |
|---|---|
| RAG 통합 테스트 | 문서 검색 결과가 실제 AI 상담 답변에 반영되는지 확인 |
| 출처 표시 테스트 | 답변에 참고 문서와 출처가 표시되는지 확인 |
| 클라우드 배포 테스트 | 배포된 프론트엔드와 백엔드가 정상 통신하는지 확인 |
| PostgreSQL 전환 테스트 | 운영 DB 환경에서 마이그레이션과 CRUD 기능 확인 |
| 보안 테스트 | 인증 없는 접근 차단, API Key 노출 여부 확인 |
| 사용자 피드백 테스트 | 답변 만족도 또는 신고 기능 추가 후 검증 |

## 13. 최종 요약

본 테스트를 통해 MediPill AI의 핵심 서비스 흐름인 `질문 입력 → API 요청 → 건강정보 기반 처리 → LLM 응답 또는 기본 안내 응답 → 답변 출력 → 상담 이력 저장` 과정이 검증되었다.

예외 케이스 중 OpenAI API Key 누락, LLM 응답 실패, 빈 질문 입력, 건강정보 미등록 상황에 대한 처리도 확인하였다.

테스트 결과 발견된 주요 문제는 OpenAI 클라이언트 생성 시점 문제와 영어 UI 문구였으며, 해당 문제는 수정 완료되었다. RAG 통합과 클라우드 배포 검증은 프로젝트의 다음 개선 단계로 진행해야 한다.
