# MediPill AI 화면설계서

## 1. 문서 개요

본 문서는 MediPill AI Web Application의 화면 구조와 사용자 흐름을 정의하기 위한 화면설계서이다. 사용자가 회원가입, 로그인, 건강정보 등록, AI 상담, 상담 이력 조회 기능을 직관적으로 사용할 수 있도록 주요 화면 요소, 레이아웃, UI 구성 방향, 상태 처리 방식을 정리한다.

본 화면설계서는 프론트엔드 구현 시 참고할 수 있도록 페이지별 구성 요소와 동작 방식을 구체적으로 작성한다.

## 2. 서비스 화면 구성

MediPill AI는 다음 5개 주요 화면으로 구성한다.

| 화면 | 경로 | 목적 |
|---|---|---|
| 로그인 화면 | `/` | 기존 사용자가 로그인한다. |
| 회원가입 화면 | `/signup` | 신규 사용자가 계정을 생성한다. |
| 건강정보 화면 | `/profile` | AI 상담에 필요한 개인 건강정보를 등록 또는 수정한다. |
| AI 상담 화면 | `/chat` | 의약품 관련 질문을 입력하고 AI 답변을 확인한다. |
| 상담 이력 화면 | `/history` | 이전 상담 내용을 다시 확인한다. |

## 3. 전체 사용자 흐름

```text
서비스 접속
  ↓
로그인 또는 회원가입
  ↓
건강정보 등록
  ↓
AI 상담 화면 이동
  ↓
질문 입력
  ↓
AI 답변 확인
  ↓
상담 이력 저장
  ↓
상담 이력 화면에서 이전 답변 확인
```

## 4. 공통 UI 설계 방향

## 4.1 전체 레이아웃

전체 화면은 사용자가 기능을 빠르게 이해할 수 있도록 단순하고 명확한 구조로 설계한다.

- 상단에는 공통 네비게이션 바를 배치한다.
- 본문은 중앙 정렬된 카드형 레이아웃을 사용한다.
- 각 페이지의 첫 영역에는 현재 단계와 화면 목적을 설명하는 제목 영역을 둔다.
- 입력 폼과 결과 영역은 시각적으로 구분한다.

## 4.2 색상 방향

의약품 상담 서비스의 신뢰감과 안정감을 주기 위해 밝은 배경과 차분한 색상을 사용한다.

| 구분 | 사용 방향 |
|---|---|
| 배경색 | 밝은 회색 또는 연한 블루 계열 |
| 주요 색상 | 블루 또는 인디고 계열 |
| 보조 색상 | 연한 민트, 회색 |
| 경고 색상 | 빨강 또는 주황 계열 |
| 성공 색상 | 초록 계열 |

색상은 페이지마다 크게 달라지지 않도록 유지하며, 버튼과 강조 요소에 동일한 주요 색상을 반복 사용한다.

## 4.3 버튼 설계

버튼은 사용자의 주요 행동을 명확히 보여주도록 설계한다.

| 버튼 유형 | 사용 위치 | 설명 |
|---|---|---|
| 기본 버튼 | 로그인, 회원가입, 질문하기, 저장 | 가장 중요한 동작에 사용한다. |
| 보조 버튼 | 건강정보 수정, 이력 보기, 이전 화면 이동 | 보조 행동에 사용한다. |
| 비활성 버튼 | 로딩 중 질문하기 버튼 | 중복 요청 방지를 위해 비활성화한다. |

## 4.4 입력창 설계

입력창은 사용자가 어떤 정보를 입력해야 하는지 명확히 알 수 있어야 한다.

- placeholder는 한국어 예시 문구를 사용한다.
- 필수 입력값은 라벨 또는 안내 문구로 구분한다.
- 긴 텍스트 입력이 필요한 항목은 textarea를 사용한다.
- 오류 발생 시 입력창 근처 또는 상단에 오류 메시지를 표시한다.

## 4.5 상태 표시

사용자 행동에 따른 상태를 명확하게 표시한다.

| 상태 | 표시 방식 |
|---|---|
| 로딩 중 | 버튼 텍스트 변경 또는 로딩 박스 표시 |
| 성공 | 저장 완료, 회원가입 완료 등 안내 메시지 |
| 실패 | 오류 메시지 또는 alert |
| 데이터 없음 | 건강정보 미등록, 상담 이력 없음 안내 |

## 5. 공통 네비게이션 설계

## 5.1 구성 요소

상단 네비게이션 바에는 다음 요소를 배치한다.

- 서비스 로고 또는 이름: `MediPill`
- 로그인
- 회원가입
- 건강정보
- AI 상담
- 상담이력
- 로그아웃 버튼

## 5.2 동작 방식

- 로고 클릭 시 AI 상담 화면으로 이동한다.
- 로그인 전에도 로그인과 회원가입 화면에 접근할 수 있다.
- 로그인 후 건강정보, AI 상담, 상담 이력 화면을 사용할 수 있다.
- 로그아웃 버튼 클릭 시 토큰을 삭제하고 로그인 화면으로 이동한다.

## 6. 로그인 화면 설계

## 6.1 화면 목적

로그인 화면은 사용자가 본인의 계정으로 서비스에 접근하기 위한 인증 화면이다.

## 6.2 화면 구성 요소

| 요소 | 설명 |
|---|---|
| 화면 제목 | 로그인 또는 서비스 시작 문구 |
| 사용자명 입력창 | 로그인 ID 입력 |
| 비밀번호 입력창 | 비밀번호 입력 |
| 로그인 버튼 | 로그인 요청 실행 |
| 회원가입 이동 링크 | 계정이 없는 사용자를 회원가입 화면으로 안내 |
| 오류 메시지 | 로그인 실패 시 표시 |

## 6.3 사용자 흐름

```text
사용자명 입력
  ↓
비밀번호 입력
  ↓
로그인 버튼 클릭
  ↓
백엔드 토큰 발급 요청
  ↓
성공 시 토큰 저장
  ↓
AI 상담 또는 건강정보 화면으로 이동
```

## 6.4 오류 안내

| 상황 | 안내 방식 |
|---|---|
| 사용자명 또는 비밀번호 누락 | 입력값을 확인하라는 메시지 표시 |
| 로그인 실패 | 아이디 또는 비밀번호가 올바르지 않다는 메시지 표시 |
| 서버 오류 | 잠시 후 다시 시도하라는 메시지 표시 |

## 7. 회원가입 화면 설계

## 7.1 화면 목적

회원가입 화면은 신규 사용자가 MediPill AI를 사용하기 위한 계정을 생성하는 화면이다.

## 7.2 화면 구성 요소

| 요소 | 설명 |
|---|---|
| 화면 제목 | 회원가입 |
| 사용자명 입력창 | 로그인에 사용할 사용자명 |
| 이메일 입력창 | 사용자 이메일 |
| 비밀번호 입력창 | 계정 비밀번호 |
| 회원가입 버튼 | 계정 생성 요청 |
| 로그인 이동 링크 | 이미 계정이 있는 사용자를 로그인 화면으로 안내 |
| 오류 메시지 | 중복 사용자명 또는 입력 오류 표시 |

## 7.3 사용자 흐름

```text
회원정보 입력
  ↓
회원가입 버튼 클릭
  ↓
백엔드 회원가입 API 요청
  ↓
성공 시 완료 메시지 표시
  ↓
로그인 화면으로 이동
```

## 7.4 오류 안내

| 상황 | 안내 방식 |
|---|---|
| 중복 사용자명 | 이미 존재하는 사용자명이라는 메시지 표시 |
| 필수 입력 누락 | 누락된 항목 입력 요청 |
| 서버 오류 | 회원가입 실패 안내 |

## 8. 건강정보 화면 설계

## 8.1 화면 목적

건강정보 화면은 AI 상담에 필요한 사용자 건강정보를 등록하거나 수정하는 화면이다. 이 화면의 데이터는 AI 답변의 개인화에 사용된다.

## 8.2 화면 구성 요소

| 요소 | 설명 |
|---|---|
| 화면 제목 | 건강정보 등록 또는 수정 |
| 나이 입력창 | 숫자 입력 |
| 성별 선택 | 남성, 여성, 기타 |
| 키 입력창 | cm 단위 입력 |
| 몸무게 입력창 | kg 단위 입력 |
| 혈액형 선택 | A, B, AB, O 등 |
| 임신/수유 여부 선택 | 해당 없음, 임신 중, 수유 중 |
| 알레르기 입력창 | 약물 또는 음식 알레르기 |
| 기저질환 입력창 | 기존 질환 정보 |
| 복용약 입력창 | 현재 복용 중인 약 |
| 저장 버튼 | 건강정보 저장 또는 수정 |

## 8.3 사용자 흐름

```text
건강정보 화면 진입
  ↓
기존 건강정보 조회
  ↓
정보가 있으면 기존 값 표시
  ↓
사용자가 정보 입력 또는 수정
  ↓
저장 버튼 클릭
  ↓
백엔드 저장 API 요청
  ↓
저장 완료 메시지 표시
```

## 8.4 입력값 설계

| 항목 | 입력 방식 | 필수 여부 |
|---|---|---|
| 나이 | 숫자 입력 | 필수 |
| 성별 | 선택 | 필수 |
| 키 | 숫자 입력 | 선택 |
| 몸무게 | 숫자 입력 | 선택 |
| 혈액형 | 선택 | 선택 |
| 임신/수유 여부 | 선택 | 선택 |
| 알레르기 | 텍스트 입력 | 선택 |
| 기저질환 | 텍스트 입력 | 선택 |
| 복용약 | 텍스트 입력 | 선택 |

## 8.5 오류 안내

| 상황 | 안내 방식 |
|---|---|
| 나이 또는 성별 누락 | 필수 건강정보를 입력하라는 메시지 표시 |
| 숫자 형식 오류 | 올바른 숫자를 입력하라는 메시지 표시 |
| 저장 실패 | 건강정보 저장 실패 메시지 표시 |

## 9. AI 상담 화면 설계

## 9.1 화면 목적

AI 상담 화면은 사용자가 의약품 복용과 관련된 질문을 입력하고, AI 답변을 확인하는 핵심 화면이다.

## 9.2 화면 구성 요소

| 요소 | 설명 |
|---|---|
| 화면 제목 | AI 의약품 상담 |
| 건강정보 요약 카드 | 등록된 건강정보 일부 표시 |
| 건강정보 수정 버튼 | 프로필 화면으로 이동 |
| 예시 질문 버튼 | 사용자가 바로 질문할 수 있는 한국어 예시 |
| 질문 입력창 | 사용자의 질문 입력 |
| 질문하기 버튼 | 질문 전송 |
| 로딩 안내 박스 | AI 답변 생성 중 표시 |
| 답변 영역 | 질문과 AI 답변 표시 |
| 상담 이력 보기 버튼 | 상담 이력 화면 이동 |

## 9.3 예시 질문 설계

예시 질문은 사용자가 어떤 방식으로 질문하면 되는지 이해하도록 돕는다.

예시:

- 타이레놀을 공복에 먹어도 되나요?
- 이부프로펜을 감기약과 같이 먹어도 되나요?
- 항생제를 복용할 때 주의할 점은 무엇인가요?

## 9.4 질문 입력창 설계

입력창 placeholder는 다음과 같이 한국어로 제공한다.

```text
예: 타이레놀을 공복에 먹어도 되나요?
```

질문 입력창은 한 줄 입력을 기본으로 하되, 추후 긴 질문 입력을 고려해 textarea로 확장할 수 있다.

## 9.5 사용자 흐름

```text
AI 상담 화면 진입
  ↓
건강정보 요약 확인
  ↓
예시 질문 선택 또는 직접 질문 입력
  ↓
질문하기 버튼 클릭
  ↓
버튼 비활성화 및 로딩 표시
  ↓
백엔드 AI 상담 API 요청
  ↓
답변 수신
  ↓
질문과 답변을 화면에 표시
  ↓
상담 이력에 저장
```

## 9.6 답변 출력 설계

답변 영역은 질문과 답변이 명확히 구분되도록 구성한다.

```text
Q. 사용자가 입력한 질문

A. AI가 생성한 답변
```

답변에는 다음 내용이 포함되는 것이 바람직하다.

- 간단 답변
- 개인 건강정보 기반 주의사항
- 추가로 확인해야 할 정보
- 전문가 상담 권고

## 9.7 오류 안내

| 상황 | 안내 방식 |
|---|---|
| 질문 미입력 | 질문을 입력하라는 메시지 표시 |
| 건강정보 미등록 | 건강정보 등록 안내와 등록 버튼 표시 |
| AI 응답 생성 실패 | 상담 요청 실패 메시지 표시 |
| 서버 오류 | 잠시 후 다시 시도하라는 메시지 표시 |

## 10. 상담 이력 화면 설계

## 10.1 화면 목적

상담 이력 화면은 사용자가 이전에 입력한 질문과 AI 답변을 다시 확인하는 화면이다.

## 10.2 화면 구성 요소

| 요소 | 설명 |
|---|---|
| 화면 제목 | 상담 이력 |
| 이력 목록 | 사용자별 상담 기록 표시 |
| 질문 영역 | 사용자가 입력한 질문 |
| 답변 영역 | AI 답변 |
| 생성 시각 | 상담 생성 날짜와 시간 |
| 데이터 없음 안내 | 상담 기록이 없을 때 표시 |

## 10.3 사용자 흐름

```text
상담 이력 화면 진입
  ↓
백엔드 이력 조회 API 요청
  ↓
상담 이력 목록 표시
  ↓
사용자가 이전 질문과 답변 확인
```

## 10.4 데이터 없음 상태

상담 이력이 없는 경우 다음과 같은 안내를 제공한다.

```text
아직 상담 이력이 없습니다.
AI 상담 화면에서 질문을 입력해보세요.
```

## 11. 주요 상태별 화면 처리

## 11.1 정상 상태

- 입력값이 정상적으로 제출된다.
- API 응답이 성공적으로 도착한다.
- 결과가 화면에 표시된다.

## 11.2 로딩 상태

- 질문 전송 후 `질문하기` 버튼을 비활성화한다.
- 버튼 텍스트를 `생성 중...`으로 변경한다.
- 별도 로딩 문구를 표시한다.

예시:

```text
건강정보를 참고해 답변을 생성하고 있습니다...
```

## 11.3 오류 상태

오류는 사용자가 이해하기 쉬운 문장으로 표시한다.

| 오류 유형 | 안내 문구 예시 |
|---|---|
| 질문 미입력 | 질문을 입력해주세요. |
| 로그인 필요 | 로그인이 필요한 기능입니다. |
| 건강정보 없음 | 건강정보가 등록되어 있지 않습니다. |
| 상담 실패 | 상담 요청에 실패했습니다. |
| 서버 오류 | 서버 오류가 발생했습니다. 잠시 후 다시 시도해주세요. |

## 12. UI 일관성 규칙

## 12.1 레이아웃 규칙

- 모든 페이지는 동일한 상단 네비게이션을 사용한다.
- 본문 콘텐츠는 중앙 정렬한다.
- 주요 기능 영역은 카드 형태로 구분한다.
- 제목, 설명, 입력, 버튼 순서로 화면을 구성한다.

## 12.2 버튼 규칙

- 주요 행동 버튼은 동일한 색상과 스타일을 사용한다.
- 보조 버튼은 기본 버튼보다 약한 색상을 사용한다.
- 위험하거나 취소 성격의 동작은 별도 색상으로 구분한다.
- 로딩 중에는 중복 클릭을 막기 위해 버튼을 비활성화한다.

## 12.3 입력창 규칙

- 입력창에는 명확한 placeholder를 제공한다.
- 필수 입력 항목은 라벨로 구분한다.
- 입력값 오류는 사용자가 바로 알 수 있도록 표시한다.

## 12.4 텍스트 규칙

- 사용자에게 보이는 문구는 한국어를 기본으로 한다.
- 의료 정보는 단정적인 진단 표현을 피한다.
- 답변 하단에는 전문가 상담 권고 문구를 포함한다.

## 13. 프론트엔드 구현 참고 사항

## 13.1 라우팅 구조

```text
/          로그인
/signup    회원가입
/profile   건강정보
/chat      AI 상담
/history   상담 이력
```

## 13.2 API 연동

프론트엔드는 Axios 인스턴스를 사용하여 Django API와 통신한다.

```text
API Base URL: http://127.0.0.1:8000
```

배포 시에는 해당 주소를 환경변수로 분리하는 것이 필요하다.

## 13.3 인증 처리

- 로그인 성공 시 access token을 localStorage에 저장한다.
- API 요청 시 Authorization 헤더에 Bearer Token을 포함한다.
- 로그아웃 시 localStorage의 토큰을 삭제한다.

## 13.4 컴포넌트 구현 방향

| 컴포넌트 | 역할 |
|---|---|
| Navbar | 페이지 이동과 로그아웃 처리 |
| Login | 로그인 폼 |
| Signup | 회원가입 폼 |
| Profile | 건강정보 입력 및 수정 폼 |
| Chat | 질문 입력, 예시 질문, AI 답변 출력 |
| History | 상담 이력 목록 출력 |

## 14. 화면 설계 요약

MediPill AI의 화면은 사용자가 `로그인 → 건강정보 등록 → AI 상담 → 상담 이력 확인` 흐름을 자연스럽게 따라갈 수 있도록 구성한다.

핵심 화면은 AI 상담 화면이며, 이 화면에서는 사용자 질문 입력, 답변 출력, 로딩 상태, 오류 안내, 건강정보 요약이 명확히 제공되어야 한다.

전체 UI는 밝은 배경, 일관된 버튼 스타일, 한국어 문구, 카드형 정보 구성을 사용하여 사용자가 의료 상담 보조 서비스라는 목적을 쉽게 이해할 수 있도록 설계한다.
